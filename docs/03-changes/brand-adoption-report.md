# 🎨 تقرير اعتماد هوية EMC V2.2 على الواجهة

> سجلّ تفصيلي لتطبيق [الهوية الرسمية V2.2](../04-references/brand/EMC_Brand_Master.v2.2.md) على `emc-frontend`.
> التاريخ: 2026-06-22 · الفرع: `feat/brand-v2.2` (منشقّ عن فرع UX) · محليّ، لم يُدفَع.

---

## المبدأ
أُعيدت الهوية **عبر إعادة تعريف الـtokens الأساسية** (لا تعديل آلاف المكوّنات يدوياً): الأسماء القديمة `customBlue/customOrange/deepBlue` أُعيد ربطها بقيم V2.2، فأُعيد كساء التطبيق كاملاً. ثمّ عولجت الألوان الصريحة (hard-coded) التي تتجاوز الـtokens، وأُنفِذت القوانين الحاكمة.

## ما طُبّق (commits)
| Commit | المحتوى |
|--------|---------|
| `54a8762` | **الأساس:** لوحة «بحر المعرفة» + «نار الشغف» (brand/accent/ink + الأسماء القديمة)، الطباعة (IBM Plex Sans Arabic للمتن · Tajawal للعناوين · IBM Plex Sans لللاتيني)، التدرّجات (العمق/الصحو/الفجر)، الحركة `cubic-bezier(0.2,0.8,0.2,1)` بلا bounce |
| `2291fbe` | **إعادة الربط:** ~1960 لون V1 مكتوب مباشرةً → V2.2 (261 ملفاً)، + إصلاح **19 مخالفة لقانون التدرّج** (بحر↔نار) |

## مصفوفة تحويل الألوان (V1 → V2.2)
| V1 | V2.2 | الدور |
|----|------|-------|
| `#2691C2` | `#0077B6` | الأزرق الأساسي (customBlue) |
| `#EC943C` | `#F28C00` | البرتقالي (customOrange) — صلب فقط |
| `#22334A` | `#0C2A4B` | الكحلي (deepBlue) |
| `#D67C28` | `#DD7C02` | برتقالي accent-600 |
| نص برتقالي على فاتح | `accent-700 = #C97208` (الجمر) | متوافق AA للعناوين |

## القوانين المُنفَّذة ✅
- ⛔ **لا مزج بحر↔نار في تدرّج واحد** — أُصلحت 19 مخالفة (أشرطة تقدّم → صحو بحري · فواصل → برتقالي صلب · خطوط زمنية وتوهّجات → بحر فقط · لافتات → عمق). البرتقالي يبقى صلباً.
- ⛔ **السماوي/البرتقالي ليسا للنص على فاتح** — نصوص الـaccent على الفاتح تستخدم الجمر `accent-700` (من عمل UX السابق).
- 🎨 تدرّج KPI وحقل الفجر أُعيدا إلى طبقات بحرية/فجرية متوافقة (لا مزج خطّي).

## تمرير البريميوم (2026-06-22)
| Commit | المحتوى |
|--------|---------|
| `017d900` | تواقيع بريميوم مشتركة (`emc-dawn`/`emc-depth`/`emc-daylight`/`emc-title-arc`/`emc-glass-premium`/`emc-tridot`) + إصلاح مخالفة تدرّج بطل الرئيسية + Footer (عنبر + الشعار + النقاط الثلاث) |
| `d175c4c` | بطل الصفحات الداخلية (سماوي/عنبر على الكحلي) |
| `8e3f071` | **تمرير 5 وكلاء:** الرئيسية + لوحات التحكم + الدورات + النماذج + المكوّنات المشتركة → بريميوم منضبط؛ + إصلاح ظلال مكسورة (مسافات rgba) في 75 ملفاً. **0 مخالفة تدرّج متبقّية.** |

المبادئ: عمق وطبقات (الفجر/العمق)، ظلال مُعايَرة (`shadow-emc*`)، حافة واحدة لكل عنصر، طباعة Tajawal للعناوين، لون متوافق مع المصفوفة، انضباط ومساحات بيضاء (لا اكتظاظ)، تواقيع متّسقة.

## التحقّق
كل commit أخضر: **0 أخطاء lint · build ناجح · 54 اختباراً**.

---

## 🔜 متبقٍّ (تحسينات هوية اختيارية — لم تُنفَّذ)
- **التواقيع الدقيقة (micro-signatures):** نمط «حقل الفجر» SVG، شريط العمود (spine) على حافة الصفحة، رمز الجناح قبل الـkicker، نقاط التذييل الثلاث، القوس المتعدّد الطبقات تحت العناوين. (عناصر تمييز متقدّمة.)
- **ذيل طويل:** ~7 توهّجات `rgba(38,145,194)` زرقاء طفيفة · بعض `hover:text-customOrange` (نص برتقالي على hover) يُفضَّل تحويله للجمر.
- **تأكيد بصري:** يُنصح بتشغيل `npm run dev` ومعاينة الصفحات للتأكّد من انسجام الكساء (خصوصاً الأبطال الداكنة والمخطّطات).

> ⚠️ لم تُغيَّر النصوص أو بيانات التواصل أو الأرقام (هوية بصرية فقط) — تحتاج تأكيدك قبل النشر.
