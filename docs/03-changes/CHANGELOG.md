# 📝 سجل التغييرات — EMC Frontend

> كل تعديل ملموس على المشروع يُسجَّل هنا بترتيب زمني عكسي (الأحدث أولاً).
> يتبع روح [Keep a Changelog](https://keepachangelog.com/ar/) و[الإصدار الدلالي](https://semver.org/lang/ar/).

الأنواع: `أُضيف` · `تغيّر` · `أُصلح` · `أُزيل` · `أمان`

---

## [غير مُصدَر] — قيد التطوير

> **المرحلة 0 (الاستقرار) منجزة** على فرع `chore/phase-0-stabilization` (محليّ، لم يُدفَع) — التفاصيل في [phase-0-stabilization.md](phase-0-stabilization.md).

### أمان
- **2026-06-21** — `npm audit fix` + حذف `i`/`sonner`: **0 ثغرات** (كانت 5، منها 3 HIGH + `dompurify` المُستخدَم فعليًّا للتعقيم).

### أُصلح
- **2026-06-21** — الأخطاء الحقيقية الثلاثة: `Date.now()` أثناء render (`lms/SessionCard`)، مكوّنات تُنشأ أثناء render (`team/DepartmentSection`+`ExecutiveSection`)، تعبير `&&` ثابت (`home/HomeCourseCard`).
- **2026-06-21** — إخضرار ESLint بالكامل: **58 خطأ → 0** (خطّاف `useNow` + تحويل الثلاثيّات إلى `if/else` + تنظيف + قرارَي إعداد مبرَّرين).
- **2026-06-21** — تصحيح `.env.example` (إزالة رابط واتساب مُسرَّب + مسافتين بادئتين + خطأ مطبعي في الفاصل).

### أُزيل
- **2026-06-21** — مخلّفات: ملف `dir` الفارغ، `favicon 2.svg` المكرّر، وتبعيتا `i` و`sonner`؛ واستبدال favicon من **493KB → ~0.65KB**.

### أُضيف
- **2026-06-21** — خطّاف `src/hooks/useNow.ts`، وبوّابة CI (`.github/workflows/ci.yml`: lint + typecheck/build + test + audit).
- **2026-06-21** — **[خطة تبسيط الواجهة (UX)](../02-planning/ux-simplification-plan.md)** — تعالج التعقيد وكثرة الخطوات والرسوم المشتِّتة.
- **2026-06-21** — **فحص عميق متعدد الوكلاء لفرع `dev`** (18 وكيلاً: 10 استكشاف + 7 تقارير + تجميع): 7 تقارير تقييم + ملخص تنفيذي + خارطة طريق + قائمة تحسينات، مبنية على قراءة الكود الفعلي (107K سطر).
- **2026-06-21** — إنشاء بنية توثيق مرجعية احترافية تحت `docs/` (4 مجلدات: التقييم، التخطيط، التعديلات، المراجع) + فهرس رئيسي.

### تغيّر
- **2026-06-22** — **تمرير بريميوم شامل** (`017d900`, `d175c4c`, `8e3f071`): تواقيع الهوية (الفجر/العمق/الصحو/القوس/الزجاج/النقاط الثلاث)، رفع الصفحة الرئيسية + لوحات التحكم + صفحات الدورات + النماذج + المكوّنات المشتركة إلى مستوى بريميوم منضبط (عمق، ظلال مُعايَرة، إيقاع، تواقيع متّسقة)، وإصلاح ظلال Tailwind المكسورة (مسافات داخل rgba) في 75 ملفاً. 0 مخالفة لقانون التدرّج. أُنشئ `.env` محلياً.
- **2026-06-22** — **اعتماد هوية EMC V2.2** على فرع `feat/brand-v2.2` (`54a8762`, `2291fbe`): لوحة «بحر المعرفة» + «نار الشغف»، طباعة Tajawal/IBM Plex Sans Arabic، تدرّجات العمق/الصحو/الفجر، حركة معتمدة؛ إعادة ربط ~1960 لون V1 صريح → V2.2 (261 ملفاً) + إصلاح 19 مخالفة لقانون «لا مزج بحر↔نار». المرجع الحاكم: [`docs/04-references/brand/`](../04-references/brand/) · التقرير: [brand-adoption-report.md](brand-adoption-report.md) · [ADR-0001](../02-planning/architecture-decisions/ADR-0001-brand-v2.2.md).
- **2026-06-22** — **تنفيذ خطة الواجهة (UX) — الذيل الطويل** (`0f8b68a`): بقية تباين النصوص البرتقالية على الفاتح، إزالة الحلقات الزائدة، وحصر التركيز في 5 نوافذ إضافية. (التقرير الكامل: [ux-implementation-report.md](ux-implementation-report.md).)
- **2026-06-22** — **UX-2/3 (تفاعلي)** (`6dd0501`): لوحة مفاتيح للملتقطات (grid/listbox، أسهم RTL)، حصر التركيز في 8 نوافذ، SubmitWorkshop بموعد واحد مطلوب، auto-fill الدولة من الهاتف، تحقّق فوري.
- **2026-06-22** — **UX-1/3/4 (دفعة)** (`fc237ca`): تباين AA، اعتماد `cn()` + توحيد حلقة التركيز، تقليل الكثافة، بنية a11y (`useFocusTrap` + النوافذ المشتركة + شريط جانبي قابل للطيّ).
- **2026-06-21** — **UX-1a** (`47c59b9`): توحيد لون الـaccent (حذف `#b9872f`)، نقل الخطوط إلى `<link>+preconnect`.
- **2026-06-21** — **UX-0 (حركة هادئة)** على فرع `feat/ux-0-calm-motion` (commit `4cb30ff`): `<MotionConfig reducedMotion="user">` يحترم تقليل الحركة عالميًّا (يغطّي 311 ملفًّا)، وتهدئة متغيّرات الكشف/التتابع المشتركة في `utils/animations.ts` (مسافة 24→8، مدد أقصر، تتابع ≈0)، وتسريع تلاشي الصفحة (0.55s→0.25s). build + lint أخضر.
- **2026-06-21** — تحويل التقييم من فرع `main` (هيكل Vite فارغ) إلى فرع **`dev`** بعد تأكيد أنه الفرع الأساسي الحقيقي؛ استُبدلت تقارير الهيكل الأولي القديمة بتقارير التطبيق الفعلي.

### ملاحظات الحالة عند فحص فرع dev (2026-06-21)
- التطبيق **ناضج وكبير**: 752 ملفاً، ~107.7K سطر، نظام LMS كامل + RBAC (20 دوراً) + تقويم + تدفّق دفع.
- 🟢 البناء أخضر (2.24s)، TypeScript يُترجَم نظيفاً، 54 اختباراً تنجح، code-splitting مُفعّل (182 `lazy()`).
- 🔴 الـ lint يفشل (3 أخطاء حقيقية + عشرات التحذيرات)، 5 ثغرات تبعيات (3 HIGH + dompurify)، لا CI، تغطية اختبار ~0.5%، لا طبقة i18n.
- 🟡 مخلّفات: حزمة `i` و`sonner` غير مستخدمتين، ملف `dir` فارغ متتبَّع، `AGENTS.md` غير ذي صلة، README/CODEBASE_ANALYSIS قديمان.

---

> 💡 **كيف تضيف مدخلاً:** أضِف سطراً تحت القسم المناسب في «غير مُصدَر» بصيغة:
> `- **YYYY-MM-DD** — وصف موجز للتغيير (#رقم_PR إن وُجد).`

- **2026-07-24** — **M0 مكتمل:** دمج 20 commit من الفريق مع طبقتنا في قاعدة موحّدة خضراء (`1748431`): 47 تعارضاً، remap مؤتمت (158 ملفاً)، 75 lint + 31 TS أُصلحت بأنواع حقيقية، 0 ثغرات، 259/259 اختباراً. [التقرير](M0-report.md)
- **2026-07-24** — **M1 مكتمل (`89facf5`):** هوية V3 نواةً — طباعة ثمانية حصرية (fallback مؤسسي)، تواقيع V3 (شريط ثلاثي/قوس مزدوج/أوراق متطايرة/أرقام شبحية)، الشعارات الرسمية بدل CSS-invert (7 مكوّنات)، الأرقام المؤكَّدة فقط علنياً + إخفاء الأقسام التجريبية، ألوان وظيفية، engines/.nvmrc.
- **2026-07-24** — **M1.5 مكتمل (`e55d6ae`+2):** حزام أمان Playwright — الرحلات الثلاث + أدوار + إشعارات (**28/28**، desktop+mobile 375px، بلا خادم عبر fixtures) + smoke بصري (8 صفحات × منفذين) + بوابة e2e في CI + قلب audit إلى blocking. اكتشف وأصلح انهيار fetchSuperAdminStats على payload خاطئ.
- **2026-07-24** — **M2a-d مكتمل:** التبسيط — قائمة 6→4 وخريطة 342 مساراً ([route-map](../01-assessment/route-map.md))، SectionHeader 4→1 (26 مستورِداً)، useFetch+timeout 20s، SectionErrorBoundary على العناقيد الكسولة، تواقيع V3 السطحية، VolunteerApply 4→2، كسّارات 375px في 8 لوحات. [التقرير](M2-report.md)
- **2026-07-25** — **M3 مكتمل (3 commits):** i18n — react-i18next بـar مصدراً، LanguageProvider يقود `<html lang dir>` بلا وميض، مبدّل لغة، **537 مفتاحاً** (تنقّل/تذييل/10 صفحات عامة/الدخول)، og:locale + hreflang ديناميكيان. EN/NL مسودّات خلف بوابة اعتماد.
- **2026-07-25** — **M4 مكتمل (`9e2d298`..`27668fc`):** الجودة — 245 مخالفة set-state-in-effect و51 react-refresh → **0/0** والقاعدتان `error`، strictNullChecks على lib+hooks+i18n+api (131 ملفاً، صفر أخطاء)، التغطية 259 → **1040 اختباراً** (43.33% على النواة الحرجة) بعتبات مانعة للتراجع، axe على كل primitive، و**15 خللاً حقيقياً** أُصلح باختبارات انحدار (منها حلقة توجيه لا نهائية وتجاوز open-redirect). [التقرير](M4-report.md)
- **2026-07-27** — **M5 مكتمل (`4359f4e` + `7cce416`):** الأداء — JS الأوّلي 411.7→**261.6KB gz** (−36%) وأكبر حزمة 215→**121.6KB** (<250 ببوابة CI `check:bundle`) بإصلاح خللَي manualChunks، إزالة preload الخطوط عديم الجدوى وIBM Plex غير حاجبة، memoization لـAuthContext (41+ مستهلكاً) والصفحات المرجعية الأربع + Navbar (TBT ≤105ms). **اكتشاف حاكم:** الخطوط غير المرخَّصة ليست في git — نشرها كما هي يكلّف ~15 نقطة و+5s LCP، فبوابة الترخيص صارت حاجز أداء. بند ≥90 أُغلق باستثناء موثَّق (سقف بنيوي CSR) وخطة prerender كبوابة مؤسس. [التقرير](M5-performance.md)
- **2026-07-27** — **M6 مكتمل (`5c93879`+):** SEO — PublicSeo على **32/32** صفحة عامة (كانت 8) بأوصاف حقيقية، بطاقة OG بهوية V3 مولَّدة آلياً، sitemap (24 مساراً) + robots من قائمة قانونية واحدة، JSON-LD مؤسسة+دورة (مؤكَّد فقط)، بوابة آلية 34 تأكيداً ضد فقدان الـmeta. **Lighthouse SEO: 100/100/100/100** (الهدف ≥95). [التقرير](M6-seo.md)
- **2026-07-27** — **M5.5 مكتمل (`7e86c6b`..`80147ae`):** التلميع البريميوم — ألبوم لقطات دائم (28 صفحة عامة + **19 لوحة دور** × منفذين = 94)، ومراجعته كشفت وأصلحت: **عتبات whileInView كسرية جعلت أقساماً كاملة غير مرئية نهائياً على الجوّال في ~14 صفحة** · 3 لوحات تنهار على العقد الفارغ + NaN في KPI الدعم (+25 اختبار انحدار) · مخالفات هوية (بانر كوكيز زجاجي، أرقام هندية/تقويم هجري، برتقالي-على-فاتح، bidi) · نصوص داخلية وهاتف وهمي وصورة Unsplash خارجية أُزيلت من العلن · empty states موحَّدة بفعل + skeletons بدل spinners + التفاف بدل قصّ العناوين. انحرافات مقبولة موثَّقة وسؤال مؤسس عن شهادات بأسماء أشخاص. [التقرير](M5.5-polish.md)
- **2026-07-27** — **دمج فريق (`4b3171f`):** مركز أتمتة البريد + جدولة المقابلة الشفوية + إدارة الدعم (`aef5c74`) — 3 تعارضات حُلّت بسياسة «وظيفة الفريق تَغلِب»، remap 29 لوناً V1→V3، البوابة كاملة خضراء.
- **2026-08-07** — **دمج فريق (`111df49`):** معهد اللغة الإنجليزية كاملاً + شجرة الحسابات + ملفات HR للمتطوعين + كوبونات checkout (12 commit، ~11 ألف سطر) — 20 ملفاً متعارضاً حُلّت بسياسة «وظيفة الفريق تَغلِب»، 57 خطأ lint في كودهم أُصلح بأنواع حقيقية (صفر disables). **إصلاح كامن حاكم:** مفاتيح tailwind المفردة (`sky`/`amber`) كانت تُميت السلالم المرقّمة في عشرات الملفات (الأصناف لا تولّد CSS إطلاقاً) → سلالم DEFAULT+قيم V3.
- **2026-08-07** — **M7 مكتمل (`0d612e1`..`455d562`):** الصلابة والحوكمة — `strict: true` على كل `src` بصفر أخطاء، التغطية **68.90%** على النطاق المنطقي الذي أقرّه المؤسس (+1129 اختباراً، الحزام 2316/2316 و**122/122** E2E) بعتبات مرفوعة على سبعة مجلدات، بوابة **hex الخام مقابل الـtokens** بنصفين (ESLint على 634 ملفاً نظيفاً + ratchet `check:brand` يمنع نمو الـ5,494 القديمة) مُتحقَّق منها بثلاث زرعات، Dependabot + جاهزية Sentry لا تعطّل، ووثيقة إصدار/نشر. **خللان حقيقيان:** ازدواج تسجيل مخالفات الاختبار على الخادم (أثر جانبي داخل مُحدِّث حالة يعيد React استدعاءه تحت StrictMode)، و**تضخّم قياس التغطية نفسه** (284 ملفاً/16,642 سطراً تسقط من المقام — 41.96% معلنة مقابل **22.35%** حقيقية) بأداة قياس بممرّتين. [التقرير](M7-report.md)
- **2026-07-25** — **M4.5 مكتمل (`732eeba`):** الأمان — وثيقة مصفوفة 10 تهديدات، تأكيد تعقيم DOMPurify للمواضع الثلاثة و83 نداء console محروسة، سكربت `check:secrets` ببوابة CI (تتبّع عمق أقواس حقيقي، تحقُّق بزرع ثم إزالة)، كتلة ترويسات nginx بـCSP Report-Only، بنود Laravel مصاغة (httpOnly شاملة broadcasting). [التقرير](M4.5-security.md)
