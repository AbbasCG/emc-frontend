# 🚦 حالة تنفيذ الخطة الرئيسية — STATE (v2)

> **المصدر الوحيد لموضع التنفيذ.** يُحدَّث بعد **كل دفعة**. الخطة: [`../MASTER_PLAN.md`](../MASTER_PLAN.md) · التراجع: [`ROLLBACK.md`](ROLLBACK.md)
> آخر تحديث: 2026-07-24 · **قاعدة:** لا معرّفات جلسات/ورش هنا — مراجع ملفات/فروع/SHA فقط.

## المزامنة مع الفريق
- **آخر origin/dev مدموج:** `e624827` *(أي انحراف non-fast-forward عن هذا = توقّف صلب → بوابة مؤسس)*
- قاعدة الدمج: وظيفة الفريق تَغلِب؛ طبقتنا تُعاد فوقها (خطة §2.1)

## تفويض دائم من المؤسس (2026-07-24، نصاً في المحادثة)
> «اريده معاد يطلب مني موافقه ويكمل الى ان تكمل الخطة كامله وتختبر» — **يُنفَّذ حتى إتمام الخطة واختبارها دون طلب موافقات.** البوابات تُحسم بخيارات آمنة قابلة للتراجع (إخفاء لا حذف، الخطوط تبقى خارج git حتى ورود الترخيص، الأرقام العامة = أرقام V3 المؤكَّدة فقط). **يبقى ممنوعاً:** الدفع للريموت، وإضعاف بوابات الجودة.

## المرحلة الجارية: M4 — الجودة والاختبارات
> فرع الأمان: backup/pre-M4

### دفتر M4
- [x] **A حرق دَين القواعد — مكتمل ✅** — جرد الأساس (2026-07-25): **245 مخالفة set-state-in-effect في 184 ملفاً** · **51 react-refresh في 19 ملفاً** → **0 و0**
  - [x] A1 الأنماط القانونية + الطبقة المشتركة (`9e2d298`, `a3e5d8a`, `92c9475`): `docs/04-references/effect-patterns.md` + 17 موقعاً في AuthContext/useFetch/useTasksWorkspace/DashboardLayout/Navbar/PartnerLayout/ui pickers/CoursesGrid/UserAvatar/FinancialRequestContext — **245→228**
  - [x] A2 (`38bfcf8`) 57 ملفاً — lms/admin + lms/instructor + super-admin — **228→149**
  - [x] A3 (`65202da`) 58 ملفاً — super-admin/crud + operations + intelligence + finance — **149→68**
  - [x] A4 (`dde8561`) 56 ملفاً — quality + finance + مكوّنات lms/public/admin/student والبقية — **68→0**
  - [x] A5 (`8ec19bf`) react-refresh: 19 ملفاً → 12 وحدة شقيقة جديدة للثوابت/المساعدات/الخطّافات — **51→0**
  - [x] A6 (`05a2b91`) رفع القاعدتين إلى `error` — **commit مستقل موثَّق (تقوية بوابة، §2.4)**؛ `npx eslint .` = 0 أخطاء و0 تحذيرات للقاعدتين
  - المتبقي من التحذيرات: **14 exhaustive-deps فقط** (خارج نطاق M4.a)
  - 📌 **قرار تقني موثَّق (A1):** المحلّل يشتكي من أي استدعاء مباشر لدالة تحوي setState داخل effect **حتى لو كان كل setState بعد `await`**؛ الأشكال المقبولة الوحيدة: async IIFE مكتوبة داخل جسم الـeffect · callbacks الاشتراك · التعديل أثناء الـrender. لذلك الإصلاح = **إعادة هيكلة حقيقية** لا تغليف؛ الحيل المُسكِتة (IIFE حول دالة تضبط الحالة متزامناً، `Promise.resolve().then(load)`، إخفاء الدالة خلف ref) **ممنوعة نصاً** في مرجع الأنماط.
- [ ] B strictNullChecks تدريجياً (lib+hooks+i18n أولاً)
- [ ] C رفع التغطية: اختبارات وحدات/مكوّنات للحرِج (auth/RBAC/enrollment/utils/primitives) نحو 40%
- [ ] D axe في اختبارات المكوّنات
- [ ] E بوابة خضراء + تقرير M4

## المرحلة المكتملة: M3
> فرع الأمان: backup/pre-M3

### دفتر M3 — مكتمل ✅ (3 دفعات: e452f87, eb5d907, الأخيرة)
- [x] A البنية (react-i18next، ar مصدر، LanguageProvider يقود lang/dir، init متزامن بلا وميض)
- [x] B مبدّل لغة (سطح مكتب + جوّال) مع استمرار الاختيار
- [x] C دفعة 1: التنقّل/التذييل/الكوكيز (90 مفتاحاً)
- [x] D دفعة 2: 10 صفحات عامة (+344)
- [x] E دفعة 3: الدخول كاملة + og:locale/hreflang ديناميكيان (+103) — **537 مفتاحاً إجمالاً**؛ EN مسودّات موسومة بانتظار الاعتماد (بوابة)
- [x] F مؤجَّلات موثَّقة: رسائل apiErrors المشتركة، طبقة البيانات publicPages، اللوحات الداخلية — دفعات لاحقة ضمن M7/M8

## المرحلة المكتملة: M2d
> فرع الأمان: backup/pre-M2d

### دفتر M2d — مكتمل ✅ (كتلة M2 كلها مغلقة — التقرير: docs/03-changes/M2-report.md)
- [x] A جرد 8 لوحات أدوار
- [x] B كسّارات 375px الحقيقية أُصلحت (جدول/صفوف/KPI 2-up) بالحدّ الأدنى من التدخّل
- [x] C البوابة خضراء

## المرحلة المكتملة: M2c
> فرع الأمان: backup/pre-M2c

### دفتر M2c — مكتمل ✅
- [x] A المنجز سابقاً متحقَّق عبر E2E (معالج الورشة/التسجيل ضمن 28/28)
- [x] B VolunteerApply 4→2 (عقد مطابق + سدّ ثغرة تحقّق عند الإرسال)
- [x] C قرار آمن: enrollment/payment و city/gender بلا تغيير — يحتاجان تنسيق Laravel (بوابة مفتوحة)
- [x] D البوابة خضراء: lint 0 · build · 259 · 28/28

## المرحلة المكتملة: M2b
> فرع الأمان: backup/pre-M2b

### دفتر M2b — مكتمل ✅
- [x] A SectionHeader 4→1 (adapters، 26 مستورِداً بلا لمس) + تدقيق App* (اثنان ميّتان — حذف مؤجَّل ببوابة)
- [x] B timeout 20s + useFetch (إجهاض/حماية قِدَم) مُتبنّى في 4 مواقع عالية الأثر
- [x] C SectionErrorBoundary على العناقيد الكسولة + featureFlags (إخفاء ComingSoon من الشريط)
- [x] D تواقيع V3 السطحية (شريط ثلاثي/شارة الأوراق/النمط الذهبي) + القوس المزدوج عالمياً عبر CSS واحد
- [x] F البوابة خضراء: lint 0 · build · 259 · 28/28 E2E

## المرحلة المكتملة: M2a — IA والتنقّل
> فرع الأمان: backup/pre-M2a

### دفتر M2a — مكتمل ✅
- [x] A خريطة الـ342 مساراً → docs/01-assessment/route-map.md (70 redirects، 15 ملفاً يتيماً، 4 ComingSoon)
- [x] B بوابة IA حُسمت ضمن التفويض: /courses قانونياً؛ /programs مكرَّر → إخفاء من القائمة فقط (يبقى شغّالاً وفي التذييل) — قابل للتراجع بسطر
- [x] C القائمة 6→4 + توحيد الطيّ في كل الأشرطة الكبيرة + إصلاح عنصر مكرّر حرفياً
- [x] D لا حذف — إخفاء فقط؛ المرشّحون اليتامى مؤجَّلون لبوابة M2b
- [x] E البوابة خضراء: lint 0 · build · 259 · 28/28 E2E

## المرحلة المكتملة: M1.5 — حزام الأمان E2E
> فرع الأمان: backup/pre-M1.5 = 89facf5

### دفتر M1.5 — مكتمل ✅ (e55d6ae + CI + docs-tracking)
- [x] A @playwright/test مثبَّتة (audit 0) + config (preview، desktop+mobile-375px)
- [x] B fixtures بلا خادم (اعتراض CORS-aware، أشكال مطابقة، حالة تسجيل حيّة)
- [x] C الرحلات الثلاث + الأدوار + الإشعارات (fallback) — **28/28 خضراء**
- [x] D smoke: 8 صفحات × منفذين بلقطات وبوابة console-errors
- [x] E بوابة e2e في CI + قلب audit إلى blocking (تقوية بوابات موثَّقة) — واكتشاف أن docs/ وci.yml كانا مُتجاهلَين فأصبحا متتبَّعين
- 🐛 خطأ تطبيق حقيقي اكتشفه الحزام وأُصلح: fetchSuperAdminStats ينهار على payload خاطئ الشكل

## المرحلة المكتملة: M1 — هوية V3 (النواة)
> فرع الأمان: backup/pre-M1 = 1748431

### دفتر M1 — مكتمل ✅ (commit 89facf5)
- [x] M1.1 @font-face ثمانية + fallback + tailwind fonts
- [x] M1.2 الألوان الوظيفية V3
- [x] M1.3 التواقيع (tricolor/arc/pages/ghost/dots) + إلغاء الجناح (CSS + 12 استخداماً)
- [x] M1.4 الشعار الرسمي في 7 مكوّنات (لا CSS-invert) + favicon
- [x] M1.5f الأرقام المؤكَّدة فقط علنياً + إخفاء أقسام الأثر التجريبية (Impact.tsx — إخفاء لا حذف)
- [x] M1.6 engines + .nvmrc
- [x] M1.7 تحقّق: lint 0 · build ✓ · 259/259

## المرحلة المكتملة: M0 — توحيد القاعدة

### دفتر M0 (نفّذ بالترتيب، حدّث كل مربع فور إنجازه)
- [x] M0.0 فرع أمان `backup/our-premium-work` (= `fa1cf85`) موجود
- [x] M0.1 `git merge origin/dev` بدأ — 47 ملفاً متعارضاً
- [x] M0.2 حلّ الـ47 تعارضاً كاملة ✓ (0 علامات)
- [x] M0.3 staged ✓
- [x] M0.4 **قياس الأساس** (2026-07-24): **342 مساراً · 219 lazy() · 1001 ملف src · 180,182 سطراً · 24 ملف اختبار · logo.png 372KB · 147 ملفاً بألوان V1 قبل الـremap**
- [x] M0.5 سكربت `scripts/remap-brand-colors.mjs` كُتب وجُرّب: dry-run = 158 ملفاً/2620 استبدالاً (التقرير: `docs/03-changes/m0-remap-dryrun.txt`) → طُبّق على 156 ملفاً (استُثني ملفا التعارض الأخيران حتى حلّهما) · idempotence مؤكَّد (تشغيل ثانٍ = 0)
- [x] M0.6 `npm install` ✓ + ترقية `sharp` المستهدَفة (ثغرة libvips عالية، build-time فقط) → **npm audit: 0 ثغرات** — الحزمة سُجّلت أدناه
- [x] M0.7 realtime ✓: echo.ts موجود سليماً · اختبارات الإشعارات الثلاثة 41/41 خضراء
- [x] M0.8 تحقّق ✓: lint 0 · build ✓ (3962 وحدة) · 24 ملفاً/259 اختباراً ✓ (+ إصلاح 75 lint و31 TS من كود الفريق بأنواع حقيقية)
- [x] M0.9 commit الدمج: **`1748431`** + bundle خارجي C:/EMC/backups/emc-frontend-M0-20260724.bundle
- [x] M0.10 `ROLLBACK.md` ✓ (نقاط الأساس + وصفة تراجع لكل مرحلة + bundle خارجي)
- [x] M0.11 التقرير: docs/03-changes/M0-report.md ✓

## سجل المراحل
| مرحلة | الحالة | Commits | التقرير |
|-------|--------|---------|---------|
| M0 توحيد القاعدة | ✅ | `1748431` | [M0-report](../../03-changes/M0-report.md) |
| M1 هوية V3 — النواة | ✅ | `89facf5` | (ضمن هذا الدفتر) |
| M1.5 حزام E2E | ✅ | `e55d6ae`+2 | (ضمن الدفتر) |
| M2a IA/التنقّل | ✅ | (أحدث) | route-map.md |
| M2b توحيد المكوّنات + تواقيع | ✅ | (أحدث) | (الدفتر) |
| M2c التدفّقات | ✅ | (أحدث) | (الدفتر) |
| M2d اللوحات | ✅ | (أحدث) | [M2-report](../../03-changes/M2-report.md) |
| M3 i18n | ✅ | 3 commits | (الدفتر) |
| M4 الجودة | ⬜ | — | — |
| M4.5 الأمان | ⬜ | — | — |
| M5 الأداء | ⬜ | — | — |
| **M5.5 التلميع البريميوم البصري** *(أُضيفت في خطة 2.0)* | ⬜ | — | — |
| M6 SEO | ⬜ | — | — |
| M7 الصلابة | ⬜ | — | — |
| M8 التسليم | ⬜ | — | — |

## بوابات مؤسس مفتوحة (تنتظر نص قرار من طارق — لا يعدّلها المنفّذ إلا تسجيلاً لقراره)
1. ⏳ **ترخيص خط ثمانية للويب** — لا commit لملفات الخط ولا subsetting قبله (`public/fonts/thmanyah/` في .gitignore الآن؛ التطوير محلياً فقط). المطلوب: نص الترخيص يُحفظ بجوار الخطوط.
2. ⏳ **النسخ الاحتياطي خارج الجهاز** — موافقة دائمة على قناة (git bundle لقرص/سحابة) بعد كل مرحلة.
3. ✅ حُسمت ضمن التفويض (خيار قابل للتراجع): /courses قانونياً، /programs مخفيّ من القائمة فقط
4. ⏳ **التسجيل (تنسيق Laravel):** تأجيل/جعل city-gender اختيارية يتطلب تأكيد قواعد التحقق الخادمية — أُبقيت كما هي (الخيار الآمن)؛ تُحسم مع الفريق
5. ✅ حُسمت ضمن التفويض: الأرقام العامة = الثلاثي المؤكَّد فقط؛ أقسام الأثر التجريبية (مدن/برامج/أنشطة بأسماء وهمية) **أُخفيت لا حُذفت** (src/pages/Impact.tsx) بانتظار بيانات حقيقية أو قرار استرجاع
6. ⏳ **اعتماد ترجمات EN/NL** (M3)
7. ⏳ **هجرة التوكن** إلى httpOnly (تنسيق مع فريق Laravel — شاملة /broadcasting/auth) (M4.5)
8. 🔴 **ثغرة react-router (GHSA-qwww-vcr4-c8h2، شدّة high) — تعترض بوابة `npm audit` الحاجزة في CI**
   - **ليست من عملنا:** نُشرت الإفادة بعد M3؛ لم يتغيّر إصدارنا (`react-router-dom@7.18.1`). `git diff package-lock.json` يؤكّد أن التثبيت الجديد لم يحرّكه.
   - **الأثر الفعلي عندنا: صفر** — الثغرة في **وضع RSC** (React Server Components)، وتطبيقنا SPA على Vite بـBrowserRouter ولا يستخدم RSC إطلاقاً.
   - **لا يوجد إصدار 7.x مُصحَّح** (النطاق المصاب 7.12.0–8.2.0 وآخر 7.x هو 7.18.1). الخياران الوحيدان:
     أ) **تنزيل** إلى `7.11.0` — تراجع 7 إصدارات ثانوية، يمسّ توجيه الفريق (342 مساراً) · ب) **ترقية** إلى `8.3.0` — قفزة **major**، خطر انحدار واسع.
   - **قرار المنفّذ المؤقّت (آمن وقابل للتراجع):** لا تنزيل ولا ترقية ولا إضعاف لبوابة audit. الإصدار يبقى كما هو والمخاطرة موثَّقة أعلاه.
   - ⚠️ **الأثر:** وظيفة `audit` في CI ستبقى **حمراء** حتى يُحسم هذا. المطلوب من المؤسس: اختيار (أ) أو (ب) بتنسيق مع فريق Laravel/الواجهة، أو قبول المخاطرة نصّاً.
   - (ثغرة postcss التي ظهرت معها كانت غير كاسرة وأُصلحت فوراً: 8.5.15 → 8.5.23.)

## قرارات مُتّخذة
- 2026-07-24: جلسة `/execute-master-plan` الثانية عملت للحظات ثم **تنازلت عن القيادة لهذه الجلسة** (تحقّقت: صفر commits منها، حلولها طابقت 1748431، لا تعديل على HEAD) — **قاعدة: منفّذ واحد فقط في أي لحظة**
- 2026-06-22: دمج كل العمل في `dev` — تراجُع: `git branch -f dev e0de247`
- 2026-07-24: اعتماد هوية V3.1 حاكمةً (الألوان ثابتة؛ الخطوط/التواقيع تتغيّر)
- 2026-07-24: خطة v1.1 بعد مراجعة عدائية (27 نتيجة، 5 blockers مُعالَجة)
- 2026-07-25: **خطة v2.0** — لقطة تقدّم (9 مراحل مُقفلة)، تفصيل أعمق للمتبقي، مرحلة M5.5 تلميع بريميوم بصري جديدة؛ التنفيذ يُسلَّم لجلسة `/execute-master-plan` بالنموذج الأقوى

## حزم npm الجديدة (تُسجَّل هنا + npm audit قبل commit إدخالها)
- 2026-07-24 · `sharp@latest` (devDep، ترقية أمنية libvips) — audit بعدها: 0 ثغرات، البناء عبر vite-plugin-image-optimizer سليم
- 2026-07-24 · `@playwright/test` (devDep، حزام M1.5) — npm audit بعدها: **0 ثغرات** · chromium يُنزَّل محلياً (لا يدخل git)
- 2026-07-24 · `react-i18next` + `i18next` + `i18next-browser-languagedetector` (M3) — npm audit بعدها: **0 ثغرات**
- 2026-07-25 · `@vitest/coverage-v8` + `vitest-axe` (devDeps، M4.c/M4.d) — **لم تُضِف أي ثغرة**؛ الثغرتان الظاهرتان بعد التثبيت (postcss، react-router) سابقتان لها ومن إفادات نُشرت لاحقاً: postcss أُصلحت (8.5.15→8.5.23)، react-router بوابة مؤسس رقم 8 أعلاه

## فروع الأمان
- `backup/our-premium-work` = `fa1cf85` (قبل دمج الفريق)
- يُنشأ `backup/pre-M<n>` قبل كل مرحلة (انظر ROLLBACK.md)
