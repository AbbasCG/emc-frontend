/**
 * Common language options for LanguagesSelect — kept in a sibling data module
 * (same convention as countryData.ts) so the component file only exports a
 * component (react-refresh/only-export-components).
 */
export const COMMON_LANGUAGES = [
  'العربية', 'الإنجليزية', 'الهولندية', 'الفرنسية', 'الألمانية', 'الإسبانية', 'التركية',
]
